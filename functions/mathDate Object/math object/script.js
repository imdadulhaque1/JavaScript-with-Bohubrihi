//-------------> Math Object

let val;
val = Math.PI;
val = Math.E;
val = Math.round(3.2);  //-----> show the integer number
val = Math.ceil(1.9);     //------> It will take the upper value and makes as integer
val = Math.floor(1.9);   //------> It will take the lower value and makes as integer
val = Math.sqrt(81);
val = Math.abs(-8);      //------> makes the positive number
val = Math.pow(2,3);
val = Math.min(7,4,2,1,8,13,9,10);   //-----> Provide the minimum numberf
val = Math.max(7,4,2,1,8,13,9,10);   //-----> Provide the Maximum numberf
val = Math.random();   //-----> Takes the random number

val = Math.floor(Math.random()*100 + 1);   //----> Provides the lower integer value and randomly among 100
console.log(val);